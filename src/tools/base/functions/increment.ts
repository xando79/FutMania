
type Tprops_increment = {
  args: any;
  pass: { increment: string };
};
export const increment = async (props: Tprops_increment) => {
  // ---------- set Caps Inputs
  const { args, pass } = props;
  let { increment } = pass;
};

