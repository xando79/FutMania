
type Tprops_decrement = {
  args: any;
  pass: { decrement: string };
};
export const decrement = async (props: Tprops_decrement) => {
  // ---------- set Caps Inputs
  const { args, pass } = props;
  let { decrement } = pass;
};

